import { OnModuleInit, OnApplicationShutdown } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Telegraf } from 'telegraf';
import { PrismaService } from '../../common/database/prisma.service';
import { UzumApiClient } from '../../uzum/client/uzum-api.client';
import { StoresService } from '../stores/stores.service';
import { TelegramStatsService } from './telegram-stats.service';
export declare class TelegramBotService implements OnModuleInit, OnApplicationShutdown {
    private readonly config;
    private readonly prisma;
    private readonly stats;
    private readonly uzum;
    private readonly stores;
    private readonly logger;
    private bot;
    private botUsername;
    private readonly drafts;
    constructor(config: ConfigService, prisma: PrismaService, stats: TelegramStatsService, uzum: UzumApiClient, stores: StoresService);
    onModuleInit(): Promise<void>;
    onApplicationShutdown(): void;
    getBot(): Telegraf | null;
    getBotUsername(): string | null;
    private notifyAdminStartup;
    private registerHandlers;
    private handleStart;
    private askPhone;
    private handleContact;
    private notifyAdminNewLink;
    private handleCallback;
    private showHome;
    private showStats;
    private showStores;
    private toggleNotify;
    private handleConfirmOrder;
    private appendToCard;
    private showExpenses;
    private handleExpenseCallback;
    private askCategory;
    private handleText;
    private saveDraft;
    private mainMenu;
    private backMenu;
    private sendHelp;
    private handleLogout;
    private render;
    private ack;
    private getDraft;
    private requireAuth;
    private findTelegramUser;
    private findUserByPhone;
    private normalizePhone;
}
